public enum NotifyType
{
    Success,
    Error
}

public enum ConnectionStatus : int
{
    Connected = 1,
    DisConnected = 2,
    NewConnection = 3
}

public enum CustomerType : int
{
    NormalCustomer = 1,
    FreeCustomer = 2,
    StakeHolder = 3
}
